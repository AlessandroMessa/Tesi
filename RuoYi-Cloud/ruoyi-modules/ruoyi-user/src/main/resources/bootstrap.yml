spring:
  application:
    name: ruoyi-user
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
      config:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
        file-extension: yml
        shared-configs:
          - dataId: application-${spring.profiles.active}.yml
            refresh: true
